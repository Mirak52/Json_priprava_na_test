<Window x:Class="PripravaNaTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PripravaNaTest"
        mc:Ignorable="d"
       
        Title="MainWindow" Height="1080" Width="1920" MinHeight="350" MinWidth="500">
    <Grid>
        <ListView Margin="0,20,1000,0" Name="People">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="userId"  DisplayMemberBinding="{Binding userId}" />
                    <GridViewColumn Header="id"  DisplayMemberBinding="{Binding id}" />
                    <GridViewColumn Header="title"  DisplayMemberBinding="{Binding title}" />
                    <GridViewColumn Header="completed"  DisplayMemberBinding="{Binding completed}" />
                   
                </GridView>
            </ListView.View>
        </ListView>
        <Label x:Name="Error" FontSize="10" Content="Label" HorizontalAlignment="Left" Margin="993,397,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
